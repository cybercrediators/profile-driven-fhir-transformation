# Canonical runtime dependencies for fsh-nifi-bridge (no ML/dev tooling).
# Optional extras live in requirements.txt (ML automapper backend, eval harness)
# and requirements-dev.txt (tests, linting).

requests>=2.32

fhir.resources>=8.0
pydantic>=2.0

valkey>=6.0

jsonpickle>=3.0
numpy>=1.26
pandas>=2.0

rich

docker>=7.0
